<UserControl x:Class="Naziki_Editor.Views.PropertyPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Naziki_Editor.Models"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300">

    <Border Background="#F0F0F0" BorderBrush="Gray" BorderThickness="1,0,0,0">
        <ContentControl x:Name="PropertyInspector">
            <ContentControl.Resources>

                <Style TargetType="TextBox">
                    <Setter Property="Background" Value="#2D2D30"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="#3F3F46"/>
                    <Setter Property="Padding" Value="4,2"/>
                </Style>

            </ContentControl.Resources>

            <TextBlock Text="请在左侧选择一个事件" Foreground="Gray" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </ContentControl>
    </Border>
</UserControl>