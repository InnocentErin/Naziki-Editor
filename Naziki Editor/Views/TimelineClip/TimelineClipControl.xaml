<UserControl x:Class="Naziki_Editor.Views.TimelineClipControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="200"
             MinHeight="30" MinWidth="10">

    <Grid x:Name="RootGrid" Background="Transparent" 
          MouseLeftButtonDown="ClipBackground_MouseDown" 
          MouseMove="ClipBackground_MouseMove" 
          MouseLeftButtonUp="ClipBackground_MouseUp">

        <Border x:Name="ClipBackground" CornerRadius="4" BorderThickness="1.5" MouseLeftButtonDown="ClipBackground_MouseDoubleClick">
            <Border.ContextMenu>
                <ContextMenu x:Name="ClipContextMenu">
                    <MenuItem Header="✨ 重新锚定至最近音符" Click="MenuReanchor_Click"/>
                    <Separator/>
                    <MenuItem Header="✂️ 剪切 (Cut)" Command="ApplicationCommands.Cut"/>
                    <MenuItem Header="📋 复制 (Copy)" Command="ApplicationCommands.Copy"/>
                    <MenuItem Header="🗑️ 删除 (Delete)" Command="ApplicationCommands.Delete"/>
                </ContextMenu>
            </Border.ContextMenu>

            <Grid>
                <Rectangle x:Name="DashBorderShape" Stroke="Transparent" StrokeThickness="1.5" RadiusX="3" RadiusY="3"/>
                <TextBlock x:Name="ClipNameText" Text="event_id" Foreground="{DynamicResource ClipTextBrush}" FontSize="10" Margin="5,2,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" IsHitTestVisible="False"/>
                <TextBlock x:Name="TxtModeIcon" Text="⏱" Foreground="{DynamicResource SecTextColor}" FontSize="10" Margin="0,2,5,0" VerticalAlignment="Top" HorizontalAlignment="Right" IsHitTestVisible="False"/>
            </Grid>
        </Border>

        <Line x:Name="VirtualEndLine" Stroke="{DynamicResource VirtualLineBrush}" StrokeThickness="1.5" StrokeDashArray="2,2" 
              X1="0" Y1="0" X2="0" Y2="40" VerticalAlignment="Stretch" Visibility="Collapsed" IsHitTestVisible="False"/>

        <Polyline x:Name="OpacityCurve" Stroke="{DynamicResource HighlightBorderColor}" StrokeThickness="2" IsHitTestVisible="False" Visibility="Collapsed"/>

        <Canvas x:Name="NodeCanvas" ClipToBounds="True" IsHitTestVisible="True"/>

        <Thumb x:Name="ResizeLeftThumb" Width="5" HorizontalAlignment="Left" Cursor="SizeWE" DragDelta="ResizeLeft_DragDelta"/>
        <Thumb x:Name="ResizeRightThumb" Width="5" HorizontalAlignment="Right" Cursor="SizeWE" DragDelta="ResizeRight_DragDelta"/>

    </Grid>
</UserControl>