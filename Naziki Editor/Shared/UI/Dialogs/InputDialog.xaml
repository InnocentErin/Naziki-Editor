<Window x:Class="Naziki_Editor.Views.Dialogs.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InputDialog" Height="160" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Background="{DynamicResource MainBgColor}"
        Foreground="{DynamicResource MainTextColor}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TxtMessage" Text="请输入内容:" 
                   FontSize="13" Margin="0,0,0,10" TextWrapping="Wrap"/>

        <TextBox Grid.Row="1" x:Name="TxtInput" 
                 FontSize="13" Padding="5,4"
                 Background="{DynamicResource MenuBgColor}" 
                 Foreground="{DynamicResource MainTextColor}"
                 BorderBrush="{DynamicResource BorderColor}"
                 BorderThickness="1"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="确定" x:Name="BtnOk" Click="BtnOk_Click" 
                    Width="70" Height="28" Margin="0,0,8,0"
                    Background="{DynamicResource AccentColor}" Foreground="White"
                    BorderThickness="0" Cursor="Hand" FontSize="13"/>
            <Button Content="取消" x:Name="BtnCancel" Click="BtnCancel_Click" 
                    Width="70" Height="28"
                    Background="{DynamicResource MenuBgColor}" Foreground="{DynamicResource MainTextColor}"
                    BorderBrush="{DynamicResource BorderColor}" Cursor="Hand" FontSize="13"/>
        </StackPanel>
    </Grid>
</Window>