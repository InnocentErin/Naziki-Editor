<Window x:Class="Naziki_Editor.Views.Dialogs.ErrorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Naziki Editor"
        WindowStyle="None"
        ResizeMode="CanResize"
        Width="820"
        Height="600"
        MinWidth="680"
        MinHeight="360"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{DynamicResource BoardBgColor}">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="8"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- 关闭按钮样式 -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource SecTextColor}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="0,6,0,0">
                            <TextBlock Text="✕" FontSize="14" 
                                       Foreground="{TemplateBinding Foreground}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 操作按钮样式 -->
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="16,4,16,4"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="{DynamicResource SideBgColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource MainTextColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" 
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource BorderColor}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource HighlightBorderColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource HighlightBorderColor}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 主按钮样式（强调色） -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource HighlightBorderColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{DynamicResource HighlightBorderColor}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1A8CD8"/>
                    <Setter Property="BorderBrush" Value="#1A8CD8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 危险按钮样式（红色） -->
        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}">
            <Setter Property="Background" Value="#D32F2F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#D32F2F"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#B71C1C"/>
                    <Setter Property="BorderBrush" Value="#B71C1C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- ============================================
         主窗口外框：带圆角和投影
         ============================================ -->
    <Border CornerRadius="8" 
            BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#80000000" BlurRadius="16" ShadowDepth="4" Opacity="0.4"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- 标题栏 -->
                <RowDefinition Height="Auto"/>  <!-- 消息内容 -->
                <RowDefinition x:Name="DetailsRow" Height="*" MinHeight="140"/>  <!-- 错误详情文本框（撑满剩余空间） -->
                <RowDefinition Height="Auto"/>  <!-- 按钮区域 -->
            </Grid.RowDefinitions>

            <!-- ============================================
                 顶部：标题栏
                 ============================================ -->
            <Border Grid.Row="0" Background="{DynamicResource SideBgColor}" CornerRadius="8,8,0,0" Height="38"
                    MouseLeftButtonDown="Window_MouseLeftButtonDown"
                    MouseMove="Window_MouseMove"
                    MouseLeftButtonUp="Window_MouseLeftButtonUp">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- 类型图标 -->
                    <TextBlock Grid.Column="0" x:Name="IconBlock"
                               Text="ℹ" FontSize="16"
                               Foreground="{DynamicResource HighlightBorderColor}"
                               VerticalAlignment="Center" Margin="14,0,6,0"/>

                    <!-- 标题文字 -->
                    <TextBlock Grid.Column="1" x:Name="TitleBlock"
                               Text="Naziki Editor"
                               Foreground="{DynamicResource MainTextColor}"
                               FontSize="13" FontWeight="SemiBold"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>

                    <!-- 关闭按钮 -->
                    <Button Grid.Column="2" x:Name="CloseBtn"
                            Style="{StaticResource CloseButtonStyle}"
                            Click="CloseBtn_Click"/>
                </Grid>
            </Border>

            <!-- ============================================
                 中部：消息内容
                 ============================================ -->
            <Border Grid.Row="1" Padding="20,16,20,8">
                <ScrollViewer MaxHeight="160"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <TextBlock x:Name="MessageBlock"
                               Text=""
                               Foreground="{DynamicResource MainTextColor}"
                               FontSize="13"
                               TextWrapping="Wrap"
                               LineHeight="20"/>
                </ScrollViewer>
            </Border>

            <!-- ============================================
                 中部：错误详情文本框（始终可见，不可编辑，可滚动）
                 ============================================ -->
            <Border Grid.Row="2" x:Name="DetailsSection" Padding="20,0,20,12" Visibility="Collapsed">
                <TextBox x:Name="DetailsTextBox"
                         Background="{DynamicResource MainBgColor}"
                         Foreground="{DynamicResource SecTextColor}"
                         BorderBrush="{DynamicResource BorderColor}"
                         BorderThickness="1"
                         FontFamily="Consolas"
                         FontSize="12"
                         IsReadOnly="True"
                         TextWrapping="NoWrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         Padding="10,8"
                         Text=""/>
            </Border>

            <!-- ============================================
                 底部：按钮区域
                 ============================================ -->
            <Border Grid.Row="3" Padding="16,12,16,16">
                <StackPanel x:Name="ButtonPanel" Orientation="Horizontal" HorizontalAlignment="Right">
                    <!-- 按钮由代码动态生成 -->
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
