<UserControl x:Class="Naziki_Editor.Views.PropertyEditor.PropertyEditor_FrameList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="{DynamicResource MainTextColor}">
    <Grid Width="231">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="🎬 状态关键帧轨道 (States)" FontWeight="Bold" Margin="5"/>

        <ListBox x:Name="ListFrames" Grid.Row="1" Margin="5" Background="Transparent" 
                 Foreground="{DynamicResource MainTextColor}" SelectionChanged="ListFrames_SelectionChanged"/>

        <Grid Grid.Row="2" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="BtnAddFrame" Content="➕ 添加关键帧" Padding="5,8" Margin="0,0,0,0" 
                    Background="#4DB8FF" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="BtnAddFrame_Click"/>
            <Button x:Name="BtnRemoveFrame" Content="🗑️ 删除关键帧" Padding="5,8" 
                    Background="#555555" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnRemoveFrame_Click" Grid.Column="1"/>
        </Grid>
    </Grid>
</UserControl>