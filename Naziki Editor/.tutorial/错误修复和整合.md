为您制定了**精确到文件的详细重构修复指令**。

---

### 总体重构计划（加载顺序修正与音频闭环）

本次修复涉及 **5 个核心文件**。请按以下顺序和细节逐一修改。

---

#### 步骤 1：修正 `AppCommands.cs`（核心加载总线）

**目标**：彻底重写 `DoLoadProject` 加载顺序，实现“先导入谱面，再加载故事板，无谱面拦截故事板”的强制逻辑。

**修改位置**：`Naziki_Editor.Core.Commands.AppCommands`

**代码修改参考**：
将现有的 `DoLoadProject` 方法替换为以下完整实现：

```csharp
public void DoLoadProject(string projectPath, NazikiProjectModel projectData, ProjectDataContext context)
{
    if (projectData == null) return;

    context.ProjectFilePath = projectPath;
    context.ProjectData = projectData;

    // ==========================================
    // 🟢 【第一优先级】：强制加载谱面 (Chart)
    // ==========================================
    bool isChartLoaded = false;
    if (!string.IsNullOrEmpty(projectData.ChartFilePath) && File.Exists(projectData.ChartFilePath))
    {
        SilentImportChart(context, projectData.ChartFilePath);
        if (context.Chart != null) isChartLoaded = true;
    }

    // ==========================================
    // 🔵 【第二优先级】：在有谱面的前提下，才允许加载故事板 (Storyboard)
    // ==========================================
    if (!string.IsNullOrEmpty(projectData.StoryboardExportPath))
    {
        if (!isChartLoaded)
        {
            // ❌ 致命拦截：没有谱面却尝试加载故事板
            _dialogService.ShowMessage(
                "🚨 加载中止：检测到工程内存在故事板文件，但未找到或无法加载对应的谱面文件！\n" +
                "为了保证故事板中所有时间锚点（如 start:noteId）的正确解析，必须优先导入谱面。\n" +
                "请检查 .nep 文件中的 ChartFilePath 路径是否正确。",
                "强制顺序加载失败", DialogMessageType.Error);
            
            // 清空故事板相关路径，防止后续渲染出错
            context.StoryboardPath = null;
            context.Storyboard = new StoryboardRoot();
            context.StoryboardMeta = new StoryboardMeta();
        }
        else
        {
            try
            {
                // 🟢 有谱面，正常解析故事板
                var result = _projectService.LoadProjectStoryboard(projectData.StoryboardExportPath, projectData);

                if (result.Storyboard != null)
                {
                    context.StoryboardPath = projectData.StoryboardExportPath;
                    context.Storyboard = result.Storyboard;
                    context.StoryboardMeta = result.Meta;

                    _historyService.Reset();
                    _historyService.RecordSnapshot(context.Storyboard);
                }
                else
                {
                    context.Storyboard = new StoryboardRoot();
                    _historyService.Reset();
                    _historyService.RecordSnapshot(context.Storyboard);
                }
            }
            catch (Exception ex)
            {
                _dialogService.ShowMessage($"读取工程内关联的故事板文件失败 QAQ：\n{ex.Message}", "同步失败");
                context.Storyboard = new StoryboardRoot();
            }
        }
    }
    else
    {
        // 无故事板路径，重置历史
        _historyService.Reset();
        _historyService.RecordSnapshot(context.Storyboard);
    }

    // ==========================================
    // 🔴 【第三优先级】：加载完成后，通知 UI 刷新
    // ==========================================
    _messageBroker.Publish("ProjectLoaded");
}
```

---

#### 步骤 2：修改 `MainWindow.xaml.cs`（支持音频自动加载与路径存盘）

**目标**：项目加载完毕时，自动检测并加载 `.nep` 中的音频文件；在用户手动导入音频时，同步将路径存入 `.nep` 并保存。

**修改位置 1：** `Naziki_Editor.Views.MainWindow` 的 `RefreshAllUIAfterProjectLoad` 方法末尾。

**代码修改参考**：

```csharp
private void RefreshAllUIAfterProjectLoad()
{
    // ... 原有刷新 UI 代码 (EventList, CanvasArea, NoteList, TimelineConsole 等) ...

    // ✨ 【新增】：项目加载完毕后，自动检查并加载音频
    if (Context.ProjectData != null && 
        !string.IsNullOrEmpty(Context.ProjectData.AudioFilePath) && 
        File.Exists(Context.ProjectData.AudioFilePath))
    {
        // 使用 async void 迅速触发，不阻塞 UI 刷新
        _ = _audioEngine.LoadAudioAsync(Context.ProjectData.AudioFilePath);
    }
}
```

**修改位置 2：** `MainWindow.xaml.cs` 的 `MenuImportAudio_Click` 方法。

**代码修改参考**：

```csharp
private async void MenuImportAudio_Click(object sender, RoutedEventArgs e)
{
    string? audioFile = _dialogService.ShowOpenFileDialog("从菜单栏选择关卡音乐", "音频文件 (*.mp3;*.wav;*.ogg)|*.mp3;*.wav;*.ogg");

    if (audioFile != null)
    {
        await _audioEngine.LoadAudioAsync(audioFile);

        // ✨ 【新增】：音频加载成功后，自动将路径写回项目数据并存盘！
        if (Context.ProjectData != null && Context.ProjectFilePath != null)
        {
            Context.ProjectData.AudioFilePath = audioFile;
            SaveProjectNepFile(); // 调用主窗口内已有的保存 Nep 方法
            _dialogService.ShowMessage("音频文件路径已自动同步保存至工程文件 (.nep)！", "路径同步成功");
        }
    }
}
```

---

#### 步骤 3：加固 `EventListControl.xaml.cs`（拦截无谱面导入故事板）

**目标**：在用户主动通过菜单或按钮“导入独立故事板”时，如果尚未导入谱面，必须强行拦截并报错（即使没有谱面也能打开，但无法显示正确时间）。

**修改位置 1：** `Naziki_Editor.Views.EventListControl` 的 `ExecuteImportStoryboard()` 方法开头。
*(注：原方法已有谱面拦截逻辑，这里需要强化一下错误提示措辞)*

**代码修改参考**：

```csharp
public void ExecuteImportStoryboard()
{
    // 🛡️ 【加固拦截】：再次确保谱面必须在场
    if (Context == null || !Context.HasChart)
    {
        _dialogService.ShowMessage("🚨 导入失败！必须先导入并加载谱面文件，才能导入故事板。\n因为故事板中的所有对象时间依赖谱面的 BPM 与时间轴计算。", 
                                   "强制顺序拦截", DialogMessageType.Error);
        return;
    }

    OpenFileDialog openFileDialog = new OpenFileDialog { Filter = "Cytoid 故事板 (*.json)|*.json|所有文件 (*.*)|*.*", Title = "请选择你的故事板文件" };

    // ... 后续原有加载逻辑保持不变 ...
}
```

---

#### 步骤 4：配合优化 `ChartTimeEngine.cs`（防范空引用）

**目标**：虽然我们在上层已经用逻辑锁死“先加载谱面”，但底层的 `ParseCytoidTimeExpression` 仍需保留强壮的防呆机制，防止非正常流程（如单测）传入 `allNotes == null` 导致崩溃。

**修改位置 1：** `Naziki_Editor.Core.ChartTimeEngine` 的 `ParseCytoidTimeExpression` 方法。

**代码修改参考**：

```csharp
public double ParseCytoidTimeExpression(object timeObj, List<C2Note> allNotes)
{
    if (timeObj == null) return 0;
    string str = timeObj.ToString().Trim();

    // 1. 如果是纯绝对秒数，直接秒解
    if (double.TryParse(str, out double directVal)) return directVal;

    // 2. 【容错增强】：当无谱面时，直接返回 float.MaxValue 标识无效时间
    if (allNotes == null || allNotes.Count == 0) return float.MaxValue;

    // 3. 有谱面则正常解析（原有逻辑保持不变）
    // ... (原有代码)
    return 0;
}
```
（*注：此步骤已在上一轮错误诊断中提及，此处明确为重构步骤之一。*）

---

### 最终修正清单总结

| 优先级          | 修改文件                   | 核心动作                                                     |
| :-------------- | :------------------------- | :----------------------------------------------------------- |
| **1. 核心逻辑** | `AppCommands.cs`           | 重写 `DoLoadProject`，强制将 `SilentImportChart` 移至 `LoadProjectStoryboard` 之前。无谱面则跳过加载故事板并弹窗报错。 |
| **2. 用户交互** | `MainWindow.xaml.cs`       | 在 `RefreshAllUIAfterProjectLoad` 中自动加载 `.nep` 内的音频。在 `MenuImportAudio_Click` 中成功加载后，自动保存音频路径至 `.nep`。 |
| **3. 安全拦截** | `EventListControl.xaml.cs` | 在 `ExecuteImportStoryboard` 中加固前置条件，无 `Context.HasChart` 则强行拦截并弹窗。 |
| **4. 底层防御** | `ChartTimeEngine.cs`       | `ParseCytoidTimeExpression` 当 `allNotes` 为空时返回 `float.MaxValue`，防止上层误判。 |
